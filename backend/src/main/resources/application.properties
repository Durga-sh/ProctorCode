spring.application.name=coding-exam-platform

# ─── Database ───
spring.datasource.url=jdbc:mysql://localhost:3306/coding_exam_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=csm@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ─── JPA / Hibernate ───
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ─── JWT ───
jwt.secret=c29tZXN1cGVyc2VjcmV0a2V5Zm9yand0YXV0aGVudGljYXRpb25jb2RpbmdleGFtcGxhdGZvcm0=
jwt.expiration=86400000

# ─── Judge0 ───
# Using the free public Judge0 CE instance (rate-limited, no Docker needed)
judge0.api.url=https://ce.judge0.com
judge0.api.timeout=30

# ─── Server ───
server.port=8080

# ─── CORS ───
app.cors.allowed-origins=http://localhost:5173
